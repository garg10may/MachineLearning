# Backend

Python/FastAPI backend workspace for machine-learning projects.

## Setup

```bash
cd backend
python3.11 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

## Projects

- `hcp_segmentation`: synthetic healthcare-provider segmentation data and future model work.
